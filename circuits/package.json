{
  "name": "@proofsight/circuits",
  "version": "0.0.1-pre-alpha",
  "description": "Zero-knowledge proof circuits for ProofSight",
  "scripts": {
    "build": "circom circuits/deposit.circom --r1cs --wasm --sym -o build && circom circuits/position_update.circom --r1cs --wasm --sym -o build && circom circuits/withdrawal.circom --r1cs --wasm --sym -o build",
    "generate-proofs": "node scripts/generate-proofs.js",
    "test": "jest",
    "benchmark": "node scripts/benchmark.js"
  },
  "keywords": [
    "zero-knowledge",
    "zk-proofs",
    "circom",
    "snarks",
    "privacy"
  ],
  "author": "ProofSight Foundation",
  "license": "MIT",
  "devDependencies": {
    "circom": "^0.5.46",
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "ffjavascript": "^0.2.58"
  }
}

